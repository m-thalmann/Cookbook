{
  "private": false,
  "scripts": {
    "api": "cd api && php artisan serve",
    "client": "cd client && npm run start -- --open",
    "prettier": "npx prettier --write .",
    "prettier-check": "npx prettier --check ."
  },
  "devDependencies": {
    "@prettier/plugin-php": "0.18.8",
    "prettier": "2.7.1"
  }
}
